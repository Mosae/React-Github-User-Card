import React from 'react';
import axios from 'axios';

const DisplayUserData = () => {
	return (
		<div className="user-card">
			<h2>User data goes here</h2>
		</div>
	);
};

export default DisplayUserData;
